{
  "name": "generate-svg-generate-svg",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/generate-svg/generate-svg/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nrwl/esbuild:esbuild",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/packages/generate-svg/generate-svg",
        "main": "packages/generate-svg/generate-svg/src/index.ts",
        "tsConfig": "packages/generate-svg/generate-svg/tsconfig.lib.json",
        "assets": ["packages/generate-svg/generate-svg/*.md"]
      }
    },
    "publish": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node tools/scripts/publish.mjs generate-svg-generate-svg {args.ver} {args.tag}"
      },
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["packages/generate-svg/generate-svg/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "packages/generate-svg/generate-svg/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
